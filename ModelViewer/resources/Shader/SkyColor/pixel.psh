struct PS_IN
{
    float4 pos  : SV_POSITION;
	float4 absPos : TEXCOOD0;
};
 
Texture2D Texture : register(t0);
SamplerState Sampler : register(s0);
 
float4 ps_skycolor( PS_IN input ) : SV_Target
{
	float3 norm = normalize(input.absPos.xyz);
	
	
	
	return ;
}